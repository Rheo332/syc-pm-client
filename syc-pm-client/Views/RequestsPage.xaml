<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="syc_pm_client.Views.RequestsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:syc_pm_client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:syc_pm_client.Viewmodels"
    d:DataContext="{d:DesignInstance Type=viewmodels:RequestsViewModel}"
    mc:Ignorable="d">

    <Grid Margin="0,31,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="20" Spacing="20">
            <!--<Button Content="Back" Command="{Binding BackCommand}" Style="{StaticResource AccentButtonStyle}" />-->
            <TextBlock Text="Pending Requests" FontSize="24" VerticalAlignment="Center" />
            <Button
                Click="Refresh_Click"
                ToolTipService.ToolTip="Refresh"
                Margin="12,4,0,0"
                Background="Transparent"
                BorderThickness="0"
                Padding="8">
                <SymbolIcon Symbol="Refresh" />
            </Button>
            <ProgressRing IsActive="{Binding IsBusy}" Width="24" Height="24" VerticalAlignment="Center" />
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding PendingRequests}" SelectedItem="{Binding SelectedRequest, Mode=TwoWay}" Margin="20,0,20,20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10" Margin="0,5" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Type}" FontWeight="Bold" />
                                <TextBlock Text=" > "  />
                                <TextBlock Text="{Binding PayloadTitle}" FontWeight="Bold" />
                            </StackPanel>
                            <TextBlock Text="{Binding Username}" />
                            <TextBlock Text="{Binding CreatedAt}" Foreground="Gray" FontSize="12" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>